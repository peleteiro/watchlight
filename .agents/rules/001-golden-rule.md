---
trigger: always_on
---

# Rule: read AGENTS.md first

Before doing anything in this repo, read `AGENTS.md` (root). It is the single
source of truth for how Watchlight works and the constraints you must follow.
Read `docs/payload.md` and its JSON Schema before touching the fetch/render code;
they define the wire contract the firmware depends on.
