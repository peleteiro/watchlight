#!/usr/bin/env bash
#MISE description="Build and open the Watchlight Wokwi project in VS Code"
set -euo pipefail

mise run simulate:build

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../../.." && pwd)"
exec code --reuse-window "$project_root" "$project_root/diagram.json"
